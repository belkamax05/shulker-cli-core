#!/usr/bin/env bash
if [ "$(basename "$SHELL")" != "zsh" ]; then
    echo "Default shell is not zsh — no change needed."
else
    echo "Default shell is zsh — switching to bash..."
    chsh -s "$(which bash)"
fi
